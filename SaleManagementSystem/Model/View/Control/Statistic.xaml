<UserControl x:Class="SaleManagementSystem.Model.View.Control.Statistic"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>

        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Text="Sales Statistics"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="DarkBlue"/>

        <!-- Bộ lọc chọn loại thống kê -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
            <TextBlock Text="Select Report Type: " FontSize="14" VerticalAlignment="Center" Margin="5"/>
            <ComboBox x:Name="cbxReportType" Width="200" SelectionChanged="cbxReportType_SelectionChanged">
                <ComboBoxItem Content="All Products"/>
                <ComboBoxItem Content="Per Product"/>
                <ComboBoxItem Content="Revenue"/>
                <ComboBoxItem Content="Employee Sales"/>
            </ComboBox>
            <Button Content="Load Data" Click="btnLoadData_Click" Margin="10,0,0,0" Padding="5,3"/>
        </StackPanel>

        <!-- Hiển thị dữ liệu -->
        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="5">
            <DataGrid x:Name="dgStatistics" AutoGenerateColumns="True"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      Margin="5"/>
        </Border>

        <!-- Tóm tắt -->
        <Border Grid.Row="3" Background="#EFEFEF" Padding="10">
            <TextBlock x:Name="txtSummary" FontSize="16" FontWeight="Bold" Text="Summary: " Foreground="DarkRed"/>
        </Border>
    </Grid>
</UserControl>
